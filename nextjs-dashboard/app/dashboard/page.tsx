// Dashboard page
// Each folder under app can become a page on the site
// (a segment, accessed via page_URL/segment)
// This file (page.tsx) returns a React component with 
// the page content, which displays when users navigate 
// to the segment
export default function Page() {
    return <p>Dashboard Page</p>;
  }